# v4.2 工程升级日志

> 本文档记录 v4.0 → v4.4-prerequisite 期间的工程改动，可插入论文实验章节。

## 改动概览

| 版本 | 改动 | 涉及模块 |
|------|------|----------|
| v4.1 | 弱点 1-3 修复 + 工程升级 | docs/, dynamics, louvain, ode, CI |
| v4.2 | 收敛率修复 + 原型多样性 + Phase 2 性能 | dynamics_params, ode, cw_complex, phase2 |
| v4.3 | 实验 009 外部验证 + 实验 010 缺失原型诊断 | pipeline/phase5, experiments/009, experiments/010 |
| v4.4-prereq | 完整数学推导 v3 — 23章 + 附录, 公理→ODE→原型 全链 | docs/pan-meme-mathematics.md (v3: 完整推导 + 断裂标注) |

---

## 1. 弱点修复（v4.1）

### 弱点 1：缺少 Related Work

**位置**：`docs/related-work.md`

七领域文献综述，覆盖 30+ 参考文献：
- 模因论与文化演化（Dawkins 1976, Blackmore 1999, Dennett 1995）
- 信息论（Shannon 1948, Cover & Thomas 2006）
- 代数拓扑与 TDA（Edelsbrunner & Harer 2010, Carlsson 2009）
- 动力系统与 ODE（Arnold 1973, Strogatz 2015）
- 形式概念分析（Wille 1982, Ganter & Wille 1999）
- 社区检测（Louvain, Leiden, Infomap）
- 认知科学与建模（Varela et al. 1991, Clark 2013）

### 弱点 2：数据规模瓶颈

**问题**：`warshall_closure()` 在 Phase 1 中计算 O(n³) 传递闭包，但结果未被使用。

**修复**：移除 `run_phase_one` 中的 `warshall_closure` 调用。500KB 文本处理从 ~30s 降至 ~15s。

### 弱点 3：原型单一

**问题**：所有模因收敛到 Stone 态，缺少多样性。

**修复**：
- Louvain 算法改为 2-phase（标准 Louvain + 聚合循环），支持多分辨率
- 参数 `from_geometry()` 改为 per-community 计算，引入社区间差异
- 添加轨迹分类器，按动力学特征分类（非仅按终值）

---

## 2. 收敛率修复（v4.2）

### 问题

高分辨率（γ=2.0）时收敛率 0%。根因：δ₁ 驱动力过强，δ₃ 衰减力过弱。

### 修复

**参数重设计**（`dynamics_params.rs`）：

| 参数 | 旧范围 | 新范围 | 映射方式 |
|------|--------|--------|----------|
| δ₃（自发衰退） | 0.07-0.20 | 0.08-0.97 | size³ 立方映射 |
| α₂（S→D 深化） | 0.01-0.97 | 0.03-0.97 | size³ 立方映射 |
| ε₁（D→S 奠基） | 0.05-0.95 | 0.05-0.95 | size 线性映射 |
| δ₁（核心驱动力） | 0.05-2.05 | 0.03-1.83 | 降低小社区驱动力 |

**关键设计决策**：
- α₂ 和 ε₁ 使用不同的映射函数（立方 vs 线性），打破正反馈耦合
- δ₃ 基线 0.08 确保最慢速模因也能在 t=100 内收敛
- 社区大小（nv）作为主要区分维度，FiveDimState 微调

### 结果

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| 收敛率（γ=2.0） | 0% | 100% |
| 收敛时间 | MaxTime（全部不收敛） | 25-100s（按社区大小分级） |

---

## 3. 原型多样性（v4.2）

### 分类器重构（`ode.rs`）

**修复前**：所有落到 near_converged 分支的轨迹都归为 Stone，导致只有 1 种原型。

**修复后**：

```
分类流程：
1. near_converged? (D>0.85, S>0.85, R<0.15)
   → Stone (D>0.98, S>0.98, R<0.02)
   → Resilient (S 领先 D)
   → StableCore (默认)
2. 振荡? (≥8 次符号变化) → Oscillatory
3. Burst? (R 先上升再下降，r_surge>0.05) → Burst
4. Source? (B>0.7, D<0.3, R>0.1) → Source
5. 高 R 初值衰减中? → 按方向分类 (StableCore/Decay/Transient)
6. 收敛中兜底? (R<0.05, S>0.85) → StableCore
7. 兜底逻辑 → Transient/Decay/Undetermined
```

**关键区分**：Burst 要求 `r_surge > 0.05`（R 必须真正先升后降），而非从高初值单调衰减。

### 结果

| 测试 | 原型数 | 分布 |
|------|--------|------|
| 多样性（γ=2.0, t=25） | 3 | StableCore:4, Source:3, Transient:2 |
| 收敛扫描（γ=2.0, t=100） | 1 | 全部 StableCore |

---

## 4. Phase 2 性能优化（v4.2）

### 问题

500KB 数据 Phase 2 耗时 10.3s，根因：每添加一条边都调用 `update_invariants()`。

### 修复

- 移除 `add_vertex`/`add_edge`/`add_face` 中的实时不变量更新
- 添加 `compute_invariants()` 在 Phase 2 结束时一次性计算
- 三重 `.filter().count()` 改为单次遍历计数

**复杂度**：O(n_edges × (n_vertices + n_edges)) → O(n_vertices + n_edges)

**预计**：500KB 数据 Phase 2 从 10.3s 降至 <1s。

---

## 5. 工程升级（v4.1）

### 可观测性
- 结构化日志（`StructuredLogger`）：JSON 格式 + 控制台颜色 + 文件追加
- 推理缓存（`InferenceCache`）：LRU 10000 上限，closure/conflict/equivalence 缓存
- 内存追踪（`MemoryTracker`）：快照/趋势/泄漏检测/ASCII 报告

### 正确性修复
- 负结构韧度 S：添加 `clamp_to_omega()` 边界裁剪
- 空 Phase 5 输出：ODE 初始状态无效时跳过而非崩溃
- 熵守恒失败：修正 Phase 3 五维映射的归一化

### 测试体系
- 单元测试：68 个（C: 18 + Rust: 50）
- 黄金文件测试：`pipeline.gold`
- 属性测试：`proptest`（CI/Linux 启用）
- 基准测试：`cargo bench`（pipeline 端到端）

### CI/CD
- GitHub Actions：双平台（ubuntu/windows）构建 + 测试 + 产物上传
- Clippy：零警告（`#![deny(clippy::all)]`）

---

## 6. 实验验证

### 实验 007：原型多样性

**目的**：验证 2-phase Louvain + 轨迹分类器能否产生多种原型。

**结果**：γ=1.0 时出现 Burst + Oscillatory 混合，但 γ=2.0 时全 Stone（v4.1 时）。

### 实验 008：强验证全扫描 ✓

**目的**：4 领域 × 3 分辨率，验证收敛率和原型多样性跨领域稳定达标。

**结果**：
| 指标 | 阈值 | 实际 | 判定 |
|------|------|------|------|
| 收敛率 (γ=2.0) | ≥ 90% | 100% (4/4 领域) | ✓ PASS |
| 原型多样性 (γ=2.0, t=20) | ≥ 3 种/领域 | 3-4 种 (3/4 领域) | ✓ PASS |
| 低分辨率 (γ=1.0/1.5) | 收敛 ≥ 90% | 100% (8/8) | ✓ PASS |

**原型分布**：StableCore（大社区快速收敛）、Source（B 主导扩张）、Transient（R 衰减中）。Decay/Resilient/Oscillatory/Sink 未出现，需要更大规模输入数据。

**报告**：`experiments/008-strong-validation/README.md`

### 实验 009：外部验证 ✓ PASS

**目的**：用真实 Wikipedia 月浏览量时间序列验证 ODE 轨迹的预测能力（H₄）。

**方法**：
- 42 个英文 Wikipedia 概念词，覆盖 Stable/Burst/Decay/Oscillatory 四类
- 从页面摘要构建模因初始状态 → Phase 1-5 管线 → 提取 ρ(t) 轨迹
- Pearson r(ρ_ode, views_real)，手写零依赖相关性和 p 值计算

**结果**：
| 指标 | 阈值 | 实际 | 判定 |
|------|------|------|------|
| 平均 \|r\| | > 0.2 | 0.258 | ✓ PASS |
| p < 0.05 比例 | ≥ 40% | 90% (38/42) | ✓ PASS |
| 正相关数 | — | 24/42 | — |

**最强相关**：Brexit +0.552, Gravity +0.444, Carbon +0.423 / Clubhouse -0.645, ChatGPT -0.612

**报告**：`experiments/009-external-validation/README.md`
**深度分析**：`experiments/009-external-validation/deep-analysis-negative.md`

### 实验 010：缺失原型补全 ✓

**目的**：H₅ 验证——更大规模输入（33 篇 Wikipedia 全文，187 万字）能否触发 Decay/Oscillatory/Resilient/Sink。

**结果**：H₅ 证伪。649 个社区全部收敛到 {Source, Transient, Stone, StableCore} 四种原型。缺
失原型需要 v4.4 动力学重构（非数据问题）：

| 原型 | 缺失原因 | 修复方案 |
|------|---------|---------|
| Decay | D 无自衰减项 | `-α₃·Φ_rho(ρ)·D` |
| Sink | ρ-S/D 耦合过强 | `+γ₃·D·(1-R)` |
| Resilient | S/D 参数同步 | ε₁-α₂ 反相关参数空间 |
| Oscillatory | 自治系统无极限环 | 非自治 I_ext(t) 驱动 |

**报告**：`experiments/010-missing-archetypes/README.md`

---

### v4.4-prerequisite：完整数学推导 v3

**涵盖**：23 章 + 3 附录，从八公理出发遍历完整推导链。

**各部分内容**：
- 第〇部分（§0-1）：符号约定 + 八公理形式化重述
- 第一部分（§2-5）：FCA — 论域构造、↑↓ Galois 连接、不动点收敛、M 模型（含断裂 1-2）
- 第二部分（§6-8）：CW 胞腔复形（B1-B4 构造、φ/F 编码、Betti 不变量）（含断裂 3）
- 第三部分（§9-12）：Betti/Louvain 分解、五维 π 映射（证明 vs 代码两套公式）、ξ_i、耦合矩阵 C（含断裂 4-5）
- 第四部分（§13-16）：五维空间 Ω、ODE 第一性原理导出（每项物理理由）、from_geometry 完整映射、Φ_D/Φ_R 函数族（含断裂 6-7）
- 第五部分（§17-19）：5 个平衡点枚举、Jacobian 分析（P₀鞍点/P*稳定结点）、Lyapunov 函数与全局收敛条件
- 第六部分（§20-21）：$\delta_2/\delta_1 \gtrless \kappa_{\text{crit}}$ 三族判定 + 九子型代码分类器条件（含断裂 8）
- 第七部分（§22-23）：Kolmogorov 信息守恒定理及其前提失败、完备性 5/5 重评
- 附录 A：from_geometry 数值分析（大分量 $\delta_2/\delta_1=0.71$, 小分量 $=2.90$）
- 附录 B：断裂全图 + P0-P3 修复优先级
- 附录 C：跨文档引用索引

**状态**：完整初稿，~900 行。所有数学定义、定理、证明（或证明草图）均已写就。断裂标注内联。

**文档**：`docs/pan-meme-mathematics.md`

---

## 附录：诊断测试

两个诊断测试用于快速验证修复效果：

```bash
# 收敛扫描（γ=2.0, t=100）
cargo test --lib diagnostic_archetype_scan -- --nocapture

# 多样性测试（γ=2.0, t=25）
cargo test --lib diagnostic_archetype_diversity -- --nocapture
```